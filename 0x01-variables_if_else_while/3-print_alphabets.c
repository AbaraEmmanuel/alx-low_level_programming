#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Printing alphabets in uppercase and lowercase'
 * Return: Always 0
 */
int main(void)
{
	int n;
	int h;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	while (m <= 90)
	{
		puthar(h);
		m++;
	}
	putchar('\n');
	return (0);
}
