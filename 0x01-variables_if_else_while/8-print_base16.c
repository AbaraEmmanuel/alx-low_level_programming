#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Printing numbers of base 6'
 * Return: Always 0
 */
int main(void)
{
	int n;
	int m;

	for (m = 97; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 102; m++)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
